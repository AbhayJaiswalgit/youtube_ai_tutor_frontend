{
  "name": "youtube-tutor-workspace",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=web",
    "dev:ext": "npm run dev --workspace=extension",
    "build": "npm run build --workspaces"
  },
  "dependencies": {
    "axios": "^1.17.0",
    "loglevel": "^1.9.2",
    "react-markdown": "^10.1.0"
  }
}
